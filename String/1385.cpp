#include <iostream>

using namespace std;

int main(){

  int N;

  string column, line;

  cin >> N;

  for(int caso = 0; caso < N; caso++){
    cin >> column;
    while(cin >> line){
      if(line[0] == 'T' && line[1] == 'P'){
        break;
      }

      
    }
  }
}